---
name: visualization-delivery
description: 可视化交付模板——提供Chart.js实用图表模板代码、仪表盘设计指引、演示汇报结构，解决可视化交付短板问题，含可直接运行的HTML代码
type: strategy-toolkit
---

# 可视化交付模板

> 再好的分析，如果呈现方式差，也会大打折扣。
> 本模块提供：可直接运行的HTML图表模板 + 仪表盘设计规范 + 演示汇报结构。

---

## 一、图表选择决策树

```
拿到数据后，先问：我想让观众看到什么？

想展示：各部分占总体的比例？
→ 饼图（≤5个类别）/ 环形图 / 堆叠柱状图

想展示：随时间变化的趋势？
→ 折线图（时间序列）

想展示：不同类别之间的大小比较？
→ 柱状图 / 条形图（类别>7个时用条形）

想展示：两个变量之间的关系？
→ 散点图（需要相关性分析时）

想展示：完成率/达成度？
→ 进度条 / 仪表盘图 / 子弹图

想展示：各渠道/各区域的业绩对比？
→ 分组柱状图 / 热力图

想展示：多维度指标的达成情况？（财务追踪仪表盘）
→ 组合图（柱状+折线）或 仪表盘矩阵

避免的图表：
❌ 3D图表（歪曲数据比例）
❌ 双Y轴图（容易误导）
❌ 超过5个系列的折线图（无法阅读）
❌ 饼图超过7个扇区（用条形图代替）
```

---

## 二、实用图表模板代码

> 所有模板均为单文件HTML，引入Chart.js CDN，无需本地安装。
> 可直接在浏览器打开，也可在Claude Code中生成。
>
> **重要：以下所有数字均为示意占位符，请替换为实际业务数据后再使用。**
> **数字不填写具体值，将无法进行有效分析。**

### 模板1：ROI三版本对比（柱状图）

```html
<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="UTF-8">
<script src="https://cdn.tailwindcss.com"></script>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<title>ROI三版本对比</title>
</head>
<body class="bg-slate-900 text-white p-8">

<h1 class="text-2xl font-bold mb-2">ROI三版本测算对比</h1>
<p class="text-slate-400 mb-6">保守 / 中性 / 乐观情景下的月度ROI对比</p>
<p class="text-amber-400 text-sm mb-4">⚠️ 示例数据，请替换为实际业务数字后再使用</p>

<div class="grid grid-cols-1 lg:grid-cols-2 gap-8">

  <!-- 图1：ROI柱状图 -->
  <div class="bg-slate-800 rounded-xl p-6">
    <h2 class="text-lg font-semibold mb-4">月度ROI对比（%）</h2>
    <canvas id="roiChart"></canvas>
  </div>

  <!-- 图2：净贡献金额 -->
  <div class="bg-slate-800 rounded-xl p-6">
    <h2 class="text-lg font-semibold mb-4">月度净贡献（$）</h2>
    <canvas id="contributionChart"></canvas>
  </div>

</div>

<script>
const CHART_DEFAULTS = {
  color: '#94a3b8',
  font: { family: 'system-ui' }
};

// ⚠️ 请替换以下占位数据为实际业务数字
const roiData = {
  labels: ['保守情景', '中性情景', '乐观情景'],
  datasets: [{
    label: '月度ROI (%)',
    data: [保守ROI, 中性ROI, 乐观ROI],   // 例: [28, 140, 300]
    backgroundColor: ['#f59e0b', '#3b82f6', '#10b981'],
    borderRadius: 8,
    barThickness: 48
  }]
};

const contributionData = {
  labels: ['保守情景', '中性情景', '乐观情景'],
  datasets: [{
    label: '增量净贡献 ($)',
    data: [保守净贡献, 中性净贡献, 乐观净贡献],   // 例: [1400, 7000, 15000]
    backgroundColor: ['#f59e0b', '#3b82f6', '#10b981'],
    borderRadius: 8
  }]
};

const commonOptions = {
  responsive: true,
  plugins: {
    legend: { display: false },
    tooltip: {
      callbacks: {
        label: ctx => `${ctx.dataset.label}: ${ctx.raw.toLocaleString()}`
      }
    }
  },
  scales: {
    y: {
      beginAtZero: true,
      grid: { color: '#334155' },
      ticks: { color: '#94a3b8' }
    },
    x: {
      grid: { display: false },
      ticks: { color: '#94a3b8' }
    }
  }
};

new Chart(document.getElementById('roiChart'), {
  type: 'bar',
  data: roiData,
  options: {
    ...commonOptions,
    plugins: {
      ...commonOptions.plugins,
      tooltip: {
        callbacks: {
          label: ctx => `ROI: ${ctx.raw}%`  // 占位，替换数字后自动更新
        }
      }
    }
  }
});

new Chart(document.getElementById('contributionChart'), {
  type: 'bar',
  data: contributionData,
  options: {
    ...commonOptions,
    plugins: {
      ...commonOptions.plugins,
      tooltip: {
        callbacks: {
          label: ctx => `净贡献: $${ctx.raw.toLocaleString()}  // 占位，替换数字后自动更新`
        }
      }
    }
  }
});
</script>
</body>
</html>
```

### 模板2：竞争格局热力图

```html
<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="UTF-8">
<script src="https://cdn.tailwindcss.com"></script>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<title>竞争格局热力图</title>
</head>
<body class="bg-slate-900 text-white p-8">

<h1 class="text-2xl font-bold mb-6">竞争态势矩阵（评分 1-5）</h1>

<div class="bg-slate-800 rounded-xl p-6">
  <canvas id="heatmapChart"></canvas>
</div>

<script src="https://cdn.jsdelivr.net/npm/chartjs-chart-matrix@2.0.1/dist/chartjs-chart-matrix.min.js"></script>
<script>
// ⚠️ 请替换为实际竞品和评分数据
const brands = ['我方品牌', '竞品A', '竞品B', '竞品C'];   // 替换为实际竞品名称
const factors = ['品牌力', '产品力', '渠道覆盖', '营销能力', '供应链', '服务'];  // 可增删因素

// 评分矩阵：brands × factors，1-5分制
// 请根据实际竞品情报填写
const scoringMatrix = [
  [本品牌各项分数],   // 例: [4, 3, 3, 5, 4, 4]
  [竞品A各项分数],    // 例: [3, 5, 3, 3, 3, 3]
  [竞品B各项分数],    // 例: [3, 3, 4, 3, 4, 3]
  [竞品C各项分数],    // 例: [2, 3, 3, 2, 3, 3]
];

const data = [];
for (let b = 0; b < brands.length; b++) {
  for (let f = 0; f < factors.length; f++) {
    data.push({
      x: f, y: b,
      v: scoringMatrix[b][f]  // 1-5分，请填入实际评分
    });
  }
}

new Chart(document.getElementById('heatmapChart'), {
  type: 'matrix',
  data: {
    datasets: [{
      label: '竞争力评分',
      data: data,
      backgroundColor: ctx => {
        const v = ctx.raw.v;
        if (v >= 4) return 'rgba(16,185,129,0.8)';
        if (v >= 3) return 'rgba(59,130,246,0.8)';
        if (v >= 2) return 'rgba(245,158,11,0.8)';
        return 'rgba(239,68,68,0.8)';
      },
      borderWidth: 0,
      width: c => c.chart.width / 6 - 4,
      height: c => c.chart.height / 5 - 4
    }]
  },
  options: {
    scales: {
      x: {
        ticks: { data: factors, color: '#94a3b8', font: {size: 12} },
        grid: { display: false },
        title: { display: true, text: '关键成功因素', color: '#64748b', font: {size: 14} }
      },
      y: {
        ticks: { data: brands, color: '#94a3b8', font: {size: 12} },
        grid: { display: false },
        title: { display: true, text: '品牌', color: '#64748b', font: {size: 14} }
      }
    },
    plugins: {
      legend: { display: false },
      tooltip: {
        callbacks: {
          label: ctx => `${brands[ctx.raw.y]} - ${factors[ctx.raw.x]}: ${ctx.raw.v}分`
        }
      }
    }
  }
});
</script>
</body>
</html>
```

### 模板3：执行追踪仪表盘

```html
<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="UTF-8">
<script src="https://cdn.tailwindcss.com"></script>
<title>方案执行追踪仪表盘</title>
</head>
<body class="bg-slate-900 text-white p-8 font-sans">

<h1 class="text-2xl font-bold mb-1">[方案名称] · 追踪仪表盘</h1>
<p class="text-slate-400 mb-2">数据截止：[YYYY-MM-DD]</p>
<p class="text-amber-400 text-sm mb-4">⚠️ 以下所有数字为示意占位符，请替换为实际追踪数据</p>

<!-- 核心KPI卡片 -->
<div class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-8">

  <div class="bg-slate-800 rounded-xl p-5 border border-slate-700">
    <div class="text-slate-400 text-sm mb-2">[指标A名称，如：市场份额]</div>
    <div class="text-3xl font-bold text-emerald-400">[当前值，如：11.2%]</div>
    <div class="text-sm text-slate-400 mt-1">目标 [X%] · 差距 [Xpp]</div>
    <div class="w-full bg-slate-700 rounded-full h-2 mt-2">
      <div class="bg-emerald-400 h-2 rounded-full" style="width:[完成率]%"></div>
    </div>
  </div>

  <div class="bg-slate-800 rounded-xl p-5 border border-slate-700">
    <div class="text-slate-400 text-sm mb-2">[指标B名称，如：新客转化率]</div>
    <div class="text-3xl font-bold text-blue-400">[当前值，如：4.1%]</div>
    <div class="text-sm text-slate-400 mt-1">目标 [X%] · 差距 [X%]</div>
    <div class="w-full bg-slate-700 rounded-full h-2 mt-2">
      <div class="bg-blue-400 h-2 rounded-full" style="width:[完成率]%"></div>
    </div>
  </div>

  <div class="bg-slate-800 rounded-xl p-5 border border-slate-700">
    <div class="text-slate-400 text-sm mb-2">[指标C名称，如：网点扩张]</div>
    <div class="text-3xl font-bold text-amber-400">[已完成/目标，如：38/50]</div>
    <div class="text-sm text-slate-400 mt-1">完成率 [X%] · [正常/预警]</div>
    <div class="w-full bg-slate-700 rounded-full h-2 mt-2">
      <div class="bg-amber-400 h-2 rounded-full" style="width:[完成率]%"></div>
    </div>
  </div>

  <div class="bg-slate-800 rounded-xl p-5 border border-red-600">
    <div class="text-red-400 text-sm mb-2">⚠️ [FSP指标名称]</div>
    <div class="text-3xl font-bold text-red-400">[当前值，如：14.3%]</div>
    <div class="text-sm text-red-300 mt-1">预警：低于底线 [X%]</div>
    <div class="w-full bg-slate-700 rounded-full h-2 mt-2">
      <div class="bg-red-400 h-2 rounded-full" style="width:[完成率]%"></div>
    </div>
    <div class="text-xs text-red-400 mt-2 font-bold">→ [FSP已触发描述]</div>
  </div>

</div>

<!-- 趋势线 -->
<div class="bg-slate-800 rounded-xl p-6 mb-6">
  <h2 class="text-lg font-semibold mb-4">核心指标月度趋势</h2>
  <div class="h-64">
    <canvas id="trendChart"></canvas>
  </div>
</div>

<!-- 风险预警 -->
<div class="bg-slate-800 rounded-xl p-6">
  <h2 class="text-lg font-semibold mb-4">⚠️ 风险预警</h2>
  <div class="space-y-3">
    <div class="flex items-center gap-3 p-3 bg-red-900/30 border border-red-800 rounded-lg">
      <div class="w-3 h-3 rounded-full bg-red-500 shrink-0"></div>
      <div>
        <div class="font-medium text-red-300">[FSP触发描述，如：净贡献率低于底线]</div>
        <div class="text-sm text-slate-400">[触发时间和处理动作]</div>
      </div>
    </div>
    <div class="flex items-center gap-3 p-3 bg-amber-900/30 border border-amber-800 rounded-lg">
      <div class="w-3 h-3 rounded-full bg-amber-500 shrink-0"></div>
      <div>
        <div class="font-medium text-amber-300">[预警事项描述]</div>
        <div class="text-sm text-slate-400">[监控计划和升级条件]</div>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
const ctx = document.getElementById('trendChart').getContext('2d');
new Chart(ctx, {
  type: 'line',
  data: {
    labels: ['[时间1]', '[时间2]', '[时间3]', '[时间4]'],  // 例: ['1月','2月','3月','4月']
    datasets: [
      {
        label: '[指标1名称，如：新客转化率] (%)',
        data: [时间1值, 时间2值, 时间3值, 时间4值],   // 例: [3.2, 3.8, 4.0, 4.1]
        borderColor: '#3b82f6',
        backgroundColor: 'rgba(59,130,246,0.1)',
        tension: 0.4,
        yAxisID: 'y',
        fill: true
      },
      {
        label: '[指标2名称，如：净贡献率] (%)',
        data: [时间1值, 时间2值, 时间3值, 时间4值],   // 例: [16.1, 15.8, 15.2, 14.3]
        borderColor: '#ef4444',
        backgroundColor: 'rgba(239,68,68,0.1)',
        tension: 0.4,
        yAxisID: 'y1',
        fill: true
      }
    ]
  },
  options: {
    responsive: true,
    maintainAspectRatio: false,
    interaction: { mode: 'index', intersect: false },
    plugins: {
      legend: { labels: { color: '#94a3b8' } },
      tooltip: { mode: 'index' }
    },
    scales: {
      x: { grid: { color: '#334155' }, ticks: { color: '#94a3b8' } },
      y: {
        type: 'linear', display: true, position: 'left',
        grid: { color: '#334155' }, ticks: { color: '#3b82f6' },
        title: { display: true, text: '[指标1名称] (%)', color: '#3b82f6' }
        // min/max根据实际数据设置
      },
      y1: {
        type: 'linear', display: true, position: 'right',
        grid: { drawOnChartArea: false },
        ticks: { color: '#ef4444' },
        title: { display: true, text: '[指标2名称] (%)', color: '#ef4444' }
        // min/max根据实际数据设置
      }
    }
  }
});
</script>
</body>
</html>
```

---

## 三、仪表盘设计规范

```
仪表盘设计五原则：

原则1：信息分层清晰
→ 第一眼：最重要的1-3个数字（KPI完成情况）
→ 第二眼：趋势/变化（红黄绿状态）
→ 第三眼：详情（点击展开）

原则2：一屏展示一个核心结论
→ 不要把所有数据堆在一起
→ 每个仪表盘只回答一个问题
→ 例："渠道扩张方案执行健康度" → 就展示这一件事的所有指标

原则3：状态用颜色而非文字
→ 颜色即含义：🟢绿=正常 / 🟡黄=预警 / 🔴红=危险
→ 不要让用户读文字才知道状态

原则4：异常比正常更显眼
→ 把问题放在视觉中心
→ 用红色/黄色突出异常，不要让它淹没在绿色里

原则5：每个指标都有"单位"
→ "11.2%"比"11.2"清晰
→ "$38,500"比"38500"易读
→ 趋势标注：↑+12% / ↓-8% / →+1%
```

---

## 四、演示汇报结构（建议框架）

```
一个完整的汇报，不是把所有分析都堆上去——
而是围绕一个核心结论，用数据讲故事。

标准汇报结构（10-15分钟）：

【开场：30秒-1分钟】
→ 不说背景，直接抛出核心结论
→ 模板：
  "这个方案/问题，我的结论是：[一句话结论]。
   支撑这个结论的三件事是：[A/B/C]。"

【第一部分：问题定义（2分钟）】
→ 不重复背景描述，直接切入你的判断
→ 模板：
  "表面问题是X，但真正的问题是Y。
   原因是：[用一句话说明根因]。
   证据是：[引用数据]。

【第二部分：关键洞察（4-6分钟）】
→ 按洞察优先级排序，不平均分配时间
→ 每个洞察：1个数据 + 1个含义 + 1个行动含义
→ 模板：
  "洞察X：[一句话]
   数据支撑：[具体数字]
   对策略的含义：[一句话]
   ↓
   所以我们建议：[具体行动]"

【第三部分：方案建议（4-6分钟）】
→ 先结论后解释
→ 优先说清楚"做什么"，其次解释"为什么"
→ 包含：ROI测算 + 风险预案
→ 模板：
  "建议：[一句话核心策略]
   ROI：[X]，投资回收期[Y个月]
   最关键的前提假设：[Z]
   如果假设不成立，我们的预案是：[方案B]"

【结尾：1分钟】
→ 总结三件事 + 提出需要决策的问题
→ 模板：
  "总结：[结论A]、[结论B]、[结论C]。
   我需要你做的决策是：[具体问题]。
   如果你同意，我们就执行；如果不同意，原因是[备选]。"
```

---

## 五、一页纸速查

```
图表选择：
比例 → 饼图（≤5类）/ 环形图
趋势 → 折线图（时间）
比较 → 柱状图（≤7类）/ 条形图（>7类）
关系 → 散点图
完成率 → 进度条 / 仪表盘
多指标 → 组合图

仪表盘五原则：
① 信息分层（一屏一结论）
② 状态用颜色（绿/黄/红）
③ 异常更显眼
④ 每个指标有单位
⑤ 第一眼最重要数字

汇报结构：
开场：30秒抛核心结论（一句话）
问题定义：2分钟，根因+证据
关键洞察：4-6分钟，按优先级排序，每条三要素
方案建议：4-6分钟，先结论后解释，含ROI+预案
结尾：1分钟，总结+需要什么决策
```
